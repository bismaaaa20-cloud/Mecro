-- ================================================================
-- [ROBLOX EXECUTOR] FULL PACK v3.0 — ALL MODULES MERGED
-- ┌─ Module A : FPS Booster (lighting, render, terrain, texture)
-- ├─ Module B : BackpackEquipBest     loop  (FireServer)    → 30s
-- ├─ Module C : RebirthPurchase       loop  (InvokeServer)  → 30s
-- ├─ Module D : IndexClaimAllRewards  loop  (InvokeServer)  → 30s
-- ├─ Module E : QuestClaimAll         loop  (InvokeServer)  → 30s
-- ├─ Module F : ClaimPlaytimeReward   loop  (1→15 seq)      → 1s/tier
-- └─ Module G : RebirthGetState       loop  (InvokeServer)  → 60s
--
-- Runtime   : Luau, Executor context
-- Platform  : Windows / Android Roblox Client
-- ================================================================

local Lighting          = game:GetService("Lighting")
local Workspace         = game:GetService("Workspace")
local ReplicatedStorage = game:GetService("ReplicatedStorage")

-- ─── Master Config ───────────────────────────────────────────────
local CFG = {
    -- [ FPS Booster ]
    STRIP_TEXTURES      = true,
    STRIP_SHADOWS       = true,
    STRIP_EFFECTS       = false,
    CHUNK_SIZE          = 150,

    -- [ Auto Loops ]
    LOOP_DELAY          = 30,  -- Module B / C / D / E → 30 detik
    PLAYTIME_DELAY      = 1,   -- Module F antar tier   → 1 detik
    PLAYTIME_CYCLE_WAIT = 2,   -- Module F jeda cycle   → 2 detik
    REBIRTH_STATE_DELAY = 60,  -- Module G              → 1 menit
}

-- ================================================================
-- MODULE A · FPS BOOSTER
-- ================================================================

local POST_FX = {
    "ColorCorrectionEffect",
    "DepthOfFieldEffect",
    "SunRaysEffect",
    "BloomEffect",
    "BlurEffect",
    "Atmosphere",
}

local function purgePostProcessing()
    for _, obj in ipairs(Lighting:GetDescendants()) do
        for _, cls in ipairs(POST_FX) do
            if obj:IsA(cls) then
                pcall(function() obj:Destroy() end)
                break
            end
        end
    end
    Lighting.DescendantAdded:Connect(function(obj)
        for _, cls in ipairs(POST_FX) do
            if obj:IsA(cls) then
                task.defer(function() pcall(function() obj:Destroy() end) end)
                break
            end
        end
    end)
end

local function downgradeLighting()
    pcall(function() Lighting.GlobalShadows            = false                         end)
    pcall(function() Lighting.Technology               = Enum.Technology.Compatibility end)
    pcall(function() Lighting.FogEnd                   = 100000                        end)
    pcall(function() Lighting.FogStart                 = 99999                         end)
    pcall(function() Lighting.EnvironmentDiffuseScale  = 0                             end)
    pcall(function() Lighting.EnvironmentSpecularScale = 0                             end)
end

local function applyRenderSettings()
    pcall(function()
        local R                   = settings().Rendering
        R.QualityLevel            = Enum.QualityLevel.Level01
        R.EagerBulkExecution      = false
        R.InterpolationThrottling = true
    end)
    pcall(function()
        local UGS             = UserSettings():GetService("UserGameSettings")
        UGS.SavedQualityLevel = Enum.SavedQualitySetting.QualityLevel01
    end)
end

local function stripTerrain()
    local T = Workspace.Terrain
    pcall(function() T.Decoration        = false end)
    pcall(function() T.WaterWaveSize     = 0     end)
    pcall(function() T.WaterWaveSpeed    = 0     end)
    pcall(function() T.WaterReflectance  = 0     end)
    pcall(function() T.WaterTransparency = 0     end)
end

local EFFECT_CLASSES = { "Fire", "Smoke", "Sparkles", "ParticleEmitter", "Trail" }

local function processInstance(obj)
    if obj:IsA("BasePart") then
        if CFG.STRIP_SHADOWS  then pcall(function() obj.CastShadow    = false                           end) end
        if CFG.STRIP_TEXTURES then pcall(function() obj.Material      = Enum.Material.SmoothPlastic     end) end
        if CFG.STRIP_TEXTURES then pcall(function() obj.RenderFidelity = Enum.RenderFidelity.Performance end) end
        return
    end
    if obj:IsA("Texture") or obj:IsA("Decal") then
        if CFG.STRIP_TEXTURES then pcall(function() obj.Transparency = 1 end) end
        return
    end
    if obj:IsA("SurfaceAppearance") then
        if CFG.STRIP_TEXTURES then pcall(function() obj:Destroy() end) end
        return
    end
    if CFG.STRIP_EFFECTS then
        for _, cls in ipairs(EFFECT_CLASSES) do
            if obj:IsA(cls) then
                pcall(function() obj:Destroy() end)
                return
            end
        end
    end
end

local function bootScan()
    local all   = Workspace:GetDescendants()
    local total = #all
    local i     = 1
    while i <= total do
        local cap = math.min(i + CFG.CHUNK_SIZE - 1, total)
        for j = i, cap do
            processInstance(all[j])
        end
        i = cap + 1
        task.wait()
    end
    Workspace.DescendantAdded:Connect(function(obj)
        task.defer(function() processInstance(obj) end)
    end)
end

task.spawn(function()
    warn("[FPS Booster] Initializing...")
    purgePostProcessing()
    downgradeLighting()
    applyRenderSettings()
    stripTerrain()
    task.spawn(bootScan)
    warn("[FPS Booster] ✓ All visual modules loaded")
end)

-- ================================================================
-- REMOTE RESOLUTION
-- ================================================================
local Remotes = ReplicatedStorage:WaitForChild("Remotes", 10)

local EquipEvent, RebirthFunc, IndexClaimRemote,
      QuestClaimRemote, PlaytimeRemote, RebirthStateRemote

if Remotes then
    EquipEvent          = Remotes:WaitForChild("BackpackEquipBest",    10)
    RebirthFunc         = Remotes:WaitForChild("RebirthPurchase",      10)
    IndexClaimRemote    = Remotes:WaitForChild("IndexClaimAllRewards", 10)
    QuestClaimRemote    = Remotes:WaitForChild("QuestClaimAll",        10)
    PlaytimeRemote      = Remotes:WaitForChild("ClaimPlaytimeReward",  10)
    RebirthStateRemote  = Remotes:WaitForChild("RebirthGetState",      10)
else
    warn("[AutoLoop] Folder 'Remotes' ga ketemu — semua loop diskip")
end

-- ================================================================
-- MODULE B · BackpackEquipBest — 30 detik
-- ================================================================
if EquipEvent then
    task.spawn(function()
        while true do
            local ok, err = pcall(function() EquipEvent:FireServer() end)
            if not ok then warn("[EquipBest] " .. tostring(err)) end
            task.wait(CFG.LOOP_DELAY)
        end
    end)
else
    warn("[EquipBest] Remote ga ketemu — diskip")
end

-- ================================================================
-- MODULE C · RebirthPurchase — 30 detik
-- ================================================================
if RebirthFunc then
    task.spawn(function()
        while true do
            local ok, err = pcall(function() return RebirthFunc:InvokeServer("Cash") end)
            if not ok then warn("[Rebirth] " .. tostring(err)) end
            task.wait(CFG.LOOP_DELAY)
        end
    end)
else
    warn("[Rebirth] Remote ga ketemu — diskip")
end

-- ================================================================
-- MODULE D · IndexClaimAllRewards — 30 detik
-- ================================================================
if IndexClaimRemote then
    task.spawn(function()
        while true do
            local ok, err = pcall(function() return IndexClaimRemote:InvokeServer() end)
            if not ok then warn("[IndexClaim] " .. tostring(err)) end
            task.wait(CFG.LOOP_DELAY)
        end
    end)
else
    warn("[IndexClaim] Remote ga ketemu — diskip")
end

-- ================================================================
-- MODULE E · QuestClaimAll — 30 detik
-- ================================================================
if QuestClaimRemote then
    task.spawn(function()
        while true do
            local ok, err = pcall(function() return QuestClaimRemote:InvokeServer() end)
            if not ok then warn("[QuestClaim] " .. tostring(err)) end
            task.wait(CFG.LOOP_DELAY)
        end
    end)
else
    warn("[QuestClaim] Remote ga ketemu — diskip")
end

-- ================================================================
-- MODULE F · ClaimPlaytimeReward Sequential (1→15) — 1s/tier
-- ================================================================
if PlaytimeRemote then
    task.spawn(function()
        while true do
            for tier = 1, 15 do
                local ok, err = pcall(function()
                    return PlaytimeRemote:InvokeServer(tier)
                end)
                if not ok then
                    warn("[Playtime] Tier " .. tier .. " reject: " .. tostring(err))
                end
                task.wait(CFG.PLAYTIME_DELAY)
            end
            warn("[Playtime] Cycle 1→15 done — restart in " .. CFG.PLAYTIME_CYCLE_WAIT .. "s")
            task.wait(CFG.PLAYTIME_CYCLE_WAIT)
        end
    end)
else
    warn("[Playtime] Remote ga ketemu — diskip")
end

-- ================================================================
-- MODULE G · RebirthGetState — 60 detik (1 menit)
-- InvokeServer(true) → server return current rebirth state
-- Di-fire lebih jarang karena ini cuma state check, bukan action
-- ================================================================
if RebirthStateRemote then
    task.spawn(function()
        while true do
            local ok, err = pcall(function()
                return RebirthStateRemote:InvokeServer(true)
            end)
            if not ok then warn("[RebirthState] " .. tostring(err)) end
            task.wait(CFG.REBIRTH_STATE_DELAY)
        end
    end)
else
    warn("[RebirthState] Remote 'RebirthGetState' ga ketemu — diskip")
end

-- ================================================================
-- TIMING SUMMARY:
--   Module B / C / D / E  →  setiap 30 detik
--   Module F               →  setiap 1 detik per tier (1→15)
--   Module G               →  setiap 60 detik (1 menit)
-- ================================================================
